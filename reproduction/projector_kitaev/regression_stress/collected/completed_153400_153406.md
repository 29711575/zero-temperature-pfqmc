# Regression/stress audit: PBS 153400--153406

All seven arrays completed: tiny enumeration 56/56, tiny ED 56/56, Gaussian exact 60/60, Gaussian QMC 60/60, HS cross-check 288/288, delta-symmetry QMC 48/48, and delta ED 8/8. There are no failed tasks in these campaigns.

## tiny exact enumeration — PASS

The largest absolute enumeration--same-contour-ED difference among `S_pi`, `S_pi_dq`, and `R_cdw` is `6.661e-15`. The largest random-configuration full-product/UDT center-Green relative difference is `5.685e-14`; the largest raw phase norm deviation is zero and the largest observable imaginary component is `1.489e-16`.

## V=0 Gaussian exact — PASS

The largest QMC--Gaussian observable difference is `4.441e-15`. Across 1200 same-configuration fast/full checks, the largest Green relative difference is `2.404e-14`, with zero eigen-sign mismatches, zero sign corrections, and maximum sign imaginary component `4.328e-13`.

## HS scheme cross-check — marginal follow-up

The two representations give compatible density observables at the sign-resolved points, with one marginal multiple-comparison outlier: `L=10,V=2,theta=18,PBC`, where `S_pi_dq(hs0)-S_pi_dq(hs1)=-1.2974e-4 +/- 3.7785e-5` (`-3.43 sigma`). This small absolute deviation should be retained for follow-up, not treated as a demonstrated implementation error.

Seven of the 48 `(physical point, scheme)` groups are sign-unresolved; all seven are `hs_scheme=0` at `L=18` and strong/long parameters. The apparently huge `L=18,V=6,theta=18,OBC` discrepancy is not interpretable: the hs=0 group has `average_sign=-0.00111 +/- 0.00498`, whereas hs=1 has average sign 1. The corresponding reweighted observables are denominator dominated.

The HS array has 22 sign-correction events in 22 tasks and maximum recorded sign imaginary component `1.969e-2`. This campaign used `diagnostic_stride=0`, so its zero stored mismatch count is not an independent direct-sign comparison and is not used to claim absence of transport mismatches.

## delta-sign symmetry — PASS

For matched seeds, the `delta=-0.6` and `delta=+0.6` density observables agree to numerical precision (largest paired absolute difference about `2.4e-10`). Same-contour QMC--ED deviations are all below `3 sigma`; the maximum magnitude is `2.48 sigma` for PBC `V=4` in `S_pi`. One sign correction occurred in 48 tasks. This array also used `diagnostic_stride=0`, so the correction is retained as a numerical warning rather than reclassified as a tested eigen-sign mismatch.

Detailed tables are `collected/exact_comparisons.csv`, `collected/paired_differences.csv`, `collected/paired_summary.csv`, and `collected/grouped_results.csv`.
